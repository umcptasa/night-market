/**
 * Component containing the information about the event
 */

import React from "react";

const SectionInfo = (props) => {
    return (
        <>
        </>
    );
}

export default SectionInfo;

